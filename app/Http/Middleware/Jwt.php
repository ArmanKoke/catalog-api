<?php

namespace App\Http\Middleware;

use Closure;

class Jwt
{
    /**
     * @param $request
     * @param Closure $next
     * @return mixed
     */
    public function handle($request, Closure $next)
    {
        return $next($request);
    }
}
